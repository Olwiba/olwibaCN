---
title: Scroll Area
description: A container with custom-styled scrollbars for consistent cross-browser scrolling.
---

<div className="flex flex-col md:flex-row items-center justify-start gap-2 mb-2">
  <CopyCommandButton command="bunx shadcn@latest add @olwibacn/scroll-area" />
</div>

<ComponentPreview name="scroll-area" />

## Installation

<InstallationTabs name="scroll-area" />

## Usage

```tsx
import { ScrollArea } from "@/components/ui/scroll-area"
```

<LiveUsageCode name="scroll-area" defaultCode={`<ScrollArea className="h-64 w-full rounded-md border font-mono text-sm">\n  <div className="overflow-x-hidden p-4">\n    {logs.map((log, i) => (\n      <div key={i} className="flex gap-6 py-1">\n        <span className="shrink-0 text-muted-foreground">{log.time}</span>\n        <span className="w-14 shrink-0">{log.level}</span>\n        <span className="min-w-0 truncate">{log.msg}</span>\n      </div>\n    ))}\n  </div>\n</ScrollArea>`} />

## API Reference

<APIReference name="ScrollArea" extends="div" props={[
  { name: "scrollbars", type: '"vertical" | "horizontal" | "both"', default: '"vertical"' },
]} />

<APIReference name="ScrollBar" extends="div" props={[
  { name: "orientation", type: '"vertical" | "horizontal"', default: '"vertical"' },
]} />
