---
title: Sheet
description: A panel that slides in from any edge of the screen as a modal overlay.
---

<div className="flex flex-col md:flex-row items-center justify-start gap-2 mb-2">
  <CopyCommandButton command="bunx shadcn@latest add @olwibacn/sheet" />
</div>

<ComponentPreview name="sheet" />

## Installation

<InstallationTabs name="sheet" />

## Usage

```tsx
import {
  Sheet,
  SheetClose,
  SheetContent,
  SheetDescription,
  SheetFooter,
  SheetHeader,
  SheetTitle,
  SheetTrigger,
} from "@/components/ui/sheet"
import { Button } from "@/components/ui/button"
import { Input } from "@/components/ui/input"
import { Label } from "@/components/ui/label"
```

<LiveUsageCode name="sheet" defaultCode={`<Sheet>\n  <SheetTrigger asChild>\n    <Button variant="outline">Open Sheet</Button>\n  </SheetTrigger>\n  <SheetContent>\n    <SheetHeader>\n      <SheetTitle>Edit profile</SheetTitle>\n      <SheetDescription>\n        Make changes to your profile here. Click save when you're done.\n      </SheetDescription>\n    </SheetHeader>\n    <div className="grid gap-4 py-4">\n      <div className="grid gap-2">\n        <Label htmlFor="name">Name</Label>\n        <Input id="name" defaultValue="Pedro Duarte" />\n      </div>\n      <div className="grid gap-2">\n        <Label htmlFor="username">Username</Label>\n        <Input id="username" defaultValue="@peduarte" />\n      </div>\n    </div>\n    <SheetFooter>\n      <SheetClose asChild>\n        <Button type="submit">Save changes</Button>\n      </SheetClose>\n    </SheetFooter>\n  </SheetContent>\n</Sheet>`} />

## API Reference

<APIReference name="Sheet" props={[
  { name: "open", type: "boolean", default: "undefined" },
  { name: "onOpenChange", type: "(open: boolean) => void", default: "undefined" },
  { name: "defaultOpen", type: "boolean", default: "false" }
]} />

<APIReference name="SheetTrigger" extends="button" props={[
  { name: "asChild", type: "boolean", default: "false" }
]} />

<APIReference name="SheetContent" extends="div" props={[
  { name: "side", type: '"top" | "right" | "bottom" | "left"', default: '"right"' },
  { name: "mode", type: '"playful" | "smooth"', default: "—" }
]} />

<APIReference name="SheetClose" extends="button" props={[
  { name: "asChild", type: "boolean", default: "false" }
]} />

<APIReference name="SheetHeader" extends="div" />

<APIReference name="SheetFooter" extends="div" />

<APIReference name="SheetTitle" extends="h2" />

<APIReference name="SheetDescription" extends="p" />

<APIReference name="SheetOverlay" extends="div" />

<APIReference name="SheetPortal" />
