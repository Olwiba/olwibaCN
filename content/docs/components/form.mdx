---
title: Form
description: Form components built on top of React Hook Form with validation and accessible error handling.
---

<div className="flex flex-col md:flex-row items-center justify-start gap-2 mb-2">
  <CopyCommandButton command="bunx shadcn@latest add @olwibacn/form" />
</div>

<ComponentPreview name="form" />

## Installation

<InstallationTabs name="form" />

## Usage

```tsx
import {
  Form,
  FormControl,
  FormDescription,
  FormField,
  FormItem,
  FormLabel,
  FormMessage,
} from "@/components/ui/form"
```

```tsx
const form = useForm<z.infer<typeof formSchema>>({
  resolver: zodResolver(formSchema),
  defaultValues: { username: "" },
})

<Form {...form}>
  <form onSubmit={form.handleSubmit(onSubmit)}>
    <FormField
      control={form.control}
      name="username"
      render={({ field }) => (
        <FormItem>
          <FormLabel>Username</FormLabel>
          <FormControl>
            <Input {...field} />
          </FormControl>
          <FormDescription>Your public display name.</FormDescription>
          <FormMessage />
        </FormItem>
      )}
    />
    <Button type="submit">Submit</Button>
  </form>
</Form>
```

## API Reference

<APIReference name="Form" props={[
  { name: "...methods", type: "UseFormReturn", default: "required" }
]} />

<APIReference name="FormField" props={[
  { name: "control", type: "Control", default: "required" },
  { name: "name", type: "string", default: "required" },
  { name: "render", type: "({ field }) => ReactNode", default: "required" }
]} />

<APIReference name="FormItem" extends="div" />

<APIReference name="FormLabel" extends="label" />

<APIReference name="FormControl" props={[
  { name: "asChild", type: "boolean", default: "true" }
]} />

<APIReference name="FormDescription" extends="p" />

<APIReference name="FormMessage" extends="p" />
