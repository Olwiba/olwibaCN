---
title: Sonner
description: Sonner component from the olwibaCN registry.
---

<div className="flex flex-col md:flex-row items-center justify-end gap-2 mb-2">
  <CopyCommandButton command="bunx shadcn@latest add @olwibacn/sonner" />
</div>

<ComponentPreview name="sonner" />

## Installation

<InstallationTabs name="sonner" />

## Usage

```tsx
import { Toaster } from "@/components/ui/sonner"
import { toast } from "sonner"
import { Button } from "@/components/ui/button"
```

```tsx
<Toaster />
<Button onClick={() => toast("Event created", { description: "Sunday at 9:00 AM" })}>
  Show Toast
</Button>
<Button onClick={() => toast.success("Saved!")}>Success</Button>
<Button onClick={() => toast.error("Something went wrong")}>Error</Button>
```

## API Reference

<APIReference name="Toaster" extends="div" props={[
  { name: "position", type: "'top-left' | 'top-right' | 'bottom-left' | 'bottom-right' | 'top-center' | 'bottom-center'", default: "'bottom-right'" },
  { name: "expand", type: "boolean", default: "false" }
]} />
