---
title: Card
description: Displays a card with header, content, and footer.
---

<div className="flex flex-col md:flex-row items-center justify-start gap-2 mb-2">
  <CopyCommandButton command="bunx shadcn@latest add @olwiba/card" />
</div>

<ComponentPreview name="card" />

## Installation

<InstallationTabs name="card" />

## Usage

```tsx
import {
  Card,
  CardContent,
  CardDescription,
  CardFooter,
  CardHeader,
  CardTitle,
} from "@/components/ui/card"
```

```tsx
<Card>
  <CardHeader>
    <CardTitle>Card Title</CardTitle>
    <CardDescription>Card Description</CardDescription>
  </CardHeader>
  <CardContent>
    <p>Card Content</p>
  </CardContent>
  <CardFooter>
    <p>Card Footer</p>
  </CardFooter>
</Card>
```

```tsx
<Card playful>...</Card>
```

```tsx
<Card smooth>...</Card>
```

## API Reference

<APIReference name="Card" extends="div" props={[
  { name: "playful", type: "boolean", default: "false", description: "Adds a slight rotation and an offset drop-shadow backdrop." },
  { name: "smooth", type: "boolean", default: "false", description: "Rounds corners more aggressively for a softer, friendlier look." }
]} />

<APIReference name="CardHeader" extends="div" />

<APIReference name="CardTitle" extends="div" />

<APIReference name="CardDescription" extends="div" />

<APIReference name="CardContent" extends="div" />

<APIReference name="CardFooter" extends="div" />
