---
title: Button
description: Displays a button or a component that looks like a button.
---

<div className="flex flex-col md:flex-row items-center justify-start gap-2 mb-2">
  <CopyCommandButton command="bunx shadcn@latest add @olwiba/button" />
</div>

<ComponentPreview name="button" />

## Installation

<InstallationTabs name="button" />

## Usage

```tsx
import { Button } from "@/components/ui/button"
```

```tsx
<Button variant="outline">Button</Button>
```

```tsx
<Button variant="outline" playful>Button</Button>
```

```tsx
<Button variant="outline" smooth>Button</Button>
```

## API Reference

<APIReference name="Button" extends="button" props={[
  { name: "variant", type: '"default" | "outline" | "ghost" | "destructive" | "secondary" | "link"', default: '"default"' },
  { name: "size", type: '"default" | "sm" | "lg" | "icon"', default: '"default"' },
  { name: "playful", type: "boolean", default: "false", description: "Adds a slight rotation and an offset drop-shadow backdrop." },
  { name: "smooth", type: "boolean", default: "false", description: "Rounds corners more aggressively for a softer, friendlier look." },
  { name: "asChild", type: "boolean", default: "false" }
]} />
